---
title: 'Program Structure'
date: '2020-09-06'
tagline: 'Learn the building blocks of a program'
---

## Header 2

### Header 3

#### Header 4

##### Header 5

###### Header 6

Regular text

Here is some `inline code`.

Codeblock:

```javascript hl=4
const foo = 0
const bar = 2

const getShouldHighlightLine = (hl) => {
  if (hl) {
    const lineNumbers = rangeParser(hl)
    return (index) => lineNumbers.includes(index + 1)
  }
  return () => false
}
```

> A blockquote with _some_ emphasis.
